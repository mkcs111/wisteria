# Wisteria Public Site

This folder is a public-only GitHub Pages deployment.

It must never contain application code, private documents, environment files, credentials, database configuration, or dashboard templates.
